import CartDisplay from "./components/CartDisplay";

export default function CartPage() {
  return (
    <main className="mt-18.25 w-full h-fit max-w-6xl mx-auto flex-1">
      <CartDisplay />
    </main>
  );
}
